"use client";
import { useEffect,useState } from "react";
import { api } from "../lib/api";

function Metric({label,value,detail}){return <div className="card metric p-6"><p className="text-sm font-bold text-emerald-100">{label}</p><p className="mt-3 text-4xl font-black">{value}</p><p className="mt-2 text-xs text-emerald-100/80">{detail}</p></div>}
export default function Dashboard(){const [data,setData]=useState(null);const [error,setError]=useState("");useEffect(()=>{api("/api/dashboard").then(setData).catch(e=>setError(e.message))},[]);return <main className="mx-auto max-w-7xl p-5 md:p-8"><section className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-black uppercase tracking-[.25em] text-amber-600">Operations overview</p><h1 className="mt-2 text-4xl font-black tracking-tight text-emerald-950 md:text-5xl">Restaurant Control Center</h1></div><div className="rounded-full bg-amber-100 px-4 py-2 text-sm font-bold text-amber-800">Live operational metrics</div></section>{error&&<div className="card mb-5 p-4 text-red-600">{error}</div>}<section className="grid gap-4 md:grid-cols-3"><Metric label="Sales Today" value={`Rp ${(data?.sales_today||0).toLocaleString("id-ID")}`} detail={`${data?.orders_today||0} orders`}/><Metric label="Audit Score" value={`${Number(data?.audit_score||0).toFixed(1)}%`} detail={`${data?.open_issues||0} open findings`}/><Metric label="Inventory Alerts" value={data?.inventory_alerts||0} detail={`${data?.total_items||0} tracked items`}/></section><section className="card mt-6 grid gap-6 p-6 md:grid-cols-2"><div><h2 className="text-xl font-black text-emerald-950">Internal Audit</h2><p className="mt-2 text-slate-600">Track cleanliness, SOP compliance, food quality and corrective findings from one workflow.</p></div><div><h2 className="text-xl font-black text-emerald-950">Inventory Health</h2><p className="mt-2 text-slate-600">Low-stock metrics are calculated from each item's reorder level for faster purchasing decisions.</p></div></section></main>}
